import { Translate2Pipe } from './translate2.pipe';

describe('Translate2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Translate2Pipe();
    expect(pipe).toBeTruthy();
  });
});
